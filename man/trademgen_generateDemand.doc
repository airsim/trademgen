/*!
\page trademgen_generateDemand
      Command-line utility for the C++ Simulated Travel Demand Generation Library

\section sec_synopsis SYNOPSIS

<b>trademgen_generateDemand</b> <tt>[--prefix] [-v|--version] [-h|--help] [-b|--builtin] [-i|--input <path-to-input>] [-l|--log <path-to-output-log-file>]</tt>

\section sec_description DESCRIPTION

\e trademgen_generateDemand is a small program showing how to use the TraDemGen library.

\e trademgen_generateDemand accepts the following options:

 \b --prefix<br>
    Show the TraDemGen installation prefix.

 \b -v, \b --version<br>
    Print the currently installed version of TraDemGen on the standard output.

 \b -h, \b --help<br>
    Produce that message and show usage.

 \b -b, \b --builtin<br>
    The sample BOM tree can be either built-in or parsed from an input
    file.  That latter must then be given with the -i/--input option.<br>

 \b -s, \b --see<br>
    Seed for the random generation. It allows to reproduce exactly any
    demand generation.<br>

 \b -d, \b --draws
    Number of runs for the demand generation.<br>

 \b -G, \b --demandgeneration
    Method used to generate the demand (i.e., the booking requests):
    Poisson Process (P) or Order Statistics (S).<br>

 \b -i, \b --input <path-to-input-file><br>
    Path (absolute or relative) of the (CSV) input file specifying
    the demand distributions.<br>

 \b -o, \b --output <path-to-output-file><br>
    Path (absolute or relative) of the (CSV) output file for the
    generated booking requests.<br>

 \b -l, \b --log <path-to-output-log-file><br>
    Path (absolute or relative) of the output log file.

See the output of the <tt>`trademgen_generateDemand --help'</tt> command for default options.


\section sec_see_also SEE ALSO
\b trademgen(1), \b trademgen_with_db(1), \b pytrademgen(1), \b trademgen-config(1), \b trademgen-library(3)


\section sec_support SUPPORT

Please report any bugs to http://github.com/airsim/trademgen/issues


\section sec_copyright COPYRIGHT

Copyright © 2009-2013 Denis Arnaud

See the COPYING file for more information on the (LGPLv2+) license, or
directly on Internet:<br>
http://www.gnu.org/licenses/old-licenses/lgpl-2.1.html

*/
